const ProductDetail = ({ navigation }) => {
    return (
        <div>
            <div
                style={{
                    backgroundColor: 'black',
                    height: 250,
                    width: 250,
                    borderRadius: '50%',
                }}
            />
        </div>
    );
};

export default ProductDetail;
